pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                sh 'git submodule init'
            }
        }
        stage('build') {
            steps {
                sh 'cd micropython/ports/unix'
                sh 'make'
            }
        }
    }
}
