pipeline {
    agent any
    stages {
        state('checkout') {
            sh 'git submodule init'
        }
        stage('build') {
            steps {
                sh 'cd micropython/ports/unix'
                sh 'make'
            }
        }
    }
}
